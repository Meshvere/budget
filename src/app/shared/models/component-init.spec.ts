import { ComponentInit } from './component-init';
import {ChangeDetectorRef} from '@angular/core';

describe('ComponentInit', () => {
    it('should create an instance', () => {
        expect(new ComponentInit()).toBeTruthy();
    });
});
