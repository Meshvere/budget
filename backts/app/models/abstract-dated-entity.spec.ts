import { AbstractDatedEntity } from './abstract-dated-entity';

describe('AbstractDatedEntity', () => {
  it('should create an instance', () => {
    expect(new AbstractDatedEntity()).toBeTruthy();
  });
});
