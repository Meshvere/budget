import { AbstractModalComponent } from './abstract-modal-component';

describe('AbstractModalComponent', () => {
    it('should create an instance', () => {
        expect(new AbstractModalComponent()).toBeTruthy();
    });
});
