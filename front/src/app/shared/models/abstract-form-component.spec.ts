import { AbstractFormComponent } from './abstract-form-component';

describe('AbstractComponent', () => {
    it('should create an instance', () => {
        expect(new AbstractFormComponent()).toBeTruthy();
    });
});
